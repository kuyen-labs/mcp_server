name: Build and Release

on:
  push:
    branches: [main, beta, alpha]
  pull_request:
    branches: [main]

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch tags
        run: git fetch --tags --force

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test

      # Same as fuul-sdk: publish via npm Trusted Publishing (OIDC). Requires the package on npmjs.com to list this
      # GitHub repo as a trusted publisher — no NPM_TOKEN in the workflow.
      # If OIDC is not set up yet, add repository secret NPM_TOKEN and set NPM_TOKEN + NODE_AUTH_TOKEN here temporarily.
      - name: Release
        if: github.event_name == 'push'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release
